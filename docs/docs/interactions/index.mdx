# Interaction Modes

The **interaction modes** refer to how the agent is used.

- `Ask` - The agent is triggered when the user requests it.
- `Listen` - The agent "observe" the notebook actions in the background and is triggered when a specific event occurs.
